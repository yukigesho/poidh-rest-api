# Use a read-only PostgreSQL user. Tables are read from the public schema.
DATABASE_URL="postgresql://<username>:<password>@<host>:<port>/<database>"
PORT=42070
# Optional: enables caching of data responses for 24 hours.
REDIS_URL="redis://localhost:6379/0"
